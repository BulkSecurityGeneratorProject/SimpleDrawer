async:
    corePoolSize: 2
    maxPoolSize: 50
    queueCapacity: 10000

spring:
    mail:
        host: localhost
        port: 25
        user:
        password:
        protocol: smtp
        tls: false
        auth: false
        from: LaneDrawer@localhost

    messageSource:
        cacheSeconds: 1

authentication:
    xauth:
        secret: myXAuthSecret
        # Token is valid 30 minutes
        tokenValidityInSeconds: 1800

swagger:
    title: API
    description:
    termsOfServiceUrl:
    contact:
    license:
    licenseUrl:
